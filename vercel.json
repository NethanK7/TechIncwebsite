{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "npm --prefix website ci --no-audit --no-fund",
  "buildCommand": "npm --prefix website run build && rm -rf .vercel/output && mkdir -p .vercel && mv website/.vercel/output .vercel/output",
  "outputDirectory": ".vercel/output"
}
